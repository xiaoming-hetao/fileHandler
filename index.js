const mkdir = require('./libs/mkdir');
const remove = require('./libs/remove');
const rmdir = require('./libs/rmdir');
const touch = require('./libs/touch');
const copy = require('./libs/copy'); 
const cut = require('./libs/cut');

module.exports = {
    mkdir,
    remove,
    rmdir,
    touch,
    copy,
    cut
}